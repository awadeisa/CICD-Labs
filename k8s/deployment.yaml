apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecom-app
  namespace: default
spec:
  replicas: 5
  selector:
    matchLabels:
      app: ecom-app
  template:
    metadata:
      labels:
        app: ecom-app
    spec:
      containers:
      - name: ecom-app
        image: eisait/multibranch-flask-app:latest
        ports:
        - containerPort: 5000